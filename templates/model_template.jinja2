from pydantic import BaseModel
from typing import Optional, Literal
from datetime import datetime

class {{ model }}(BaseModel):
    {% for field in fields %}
    {{ field.name }}: {{ field.type }}{% if field.default is defined %} = {{ field.default }}{% elif not field.required %} = None{% endif %}  # {{ field.description or "" }}
    {% endfor %}
